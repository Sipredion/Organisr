import { TodoButtonColorPipe } from './todo-button-color.pipe';

describe('TodoButtonColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoButtonColorPipe();
    expect(pipe).toBeTruthy();
  });
});
